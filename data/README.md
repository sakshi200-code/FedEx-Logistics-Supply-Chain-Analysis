# Dataset folder

This folder contains both versions of the data used in the project:

- `scms_delivery_history_raw.csv` — source data before cleaning.
- `fedex_cleaned.csv` — processed data after cleaning.

Both files are included in the repository for reproducibility.
